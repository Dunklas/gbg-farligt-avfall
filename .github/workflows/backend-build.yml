name: Build backend
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
    steps:
    - uses: dorny/paths-filter@v2
      id: filter
      with:
        filters: |
          backend:
            - 'gfa-backend/**'
  test-backend:
    needs: changes
    if: ${{ needs.changes.outputs.backend == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Do this
        run: echo 'Test and build backend'
  copy-backend:
    needs: changes
    if: ${{ needs.changes.outputs.backend == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - name: Do this
        run: echo 'Just copy backend'
